kind: cluster_auth_preference
version: v2
metadata:
  name: cluster-auth-preference
spec:
  # Primary auth via Auth0 OIDC SSO. Local auth retained for break-glass only.
  type: oidc
  connector_name: auth0
  local_auth: true
  second_factor: "on"
  webauthn:
    rp_id: "{{ teleport_domain }}"