kind: cluster_auth_preference
version: v2
metadata:
  name: cluster-auth-preference
spec:
  # Primary auth via Okta SAML SSO. Local auth retained for break-glass only.
  type: saml
  connector_name: okta
  local_auth: true
  second_factor: "on"
  webauthn:
    rp_id: "{{ teleport_domain }}"